import java.awt.Color;
import java.awt.Dimension;

import javax.swing.*;
class GUI{
    public static void main(String args[]){
       JFrame window = new JFrame("Projet d'électronique digitale");
       window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
       window.setSize(500,300);
       
       JPanel panelPress = new JPanel();
       panelPress.setBounds(50, 50, 30, 30);
       panelPress.setBackground(Color.RED);
       window.add(panelPress);
       
       JButton press = new JButton();
       press.setBackground(Color.LIGHT_GRAY);
       press.setFocusPainted(false); // Adds Button to content pane of frame
       press.setBorder(null);
       press.setText("Envoyer");
       panelPress.add(press);
       
       window.setVisible(true);
    }
}